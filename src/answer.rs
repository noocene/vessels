use futures::Future;

use base64::decode;
use vitruvia::{executor::run, network::mesh};

/*use stdweb::unstable::TryInto;

#[macro_use]
extern crate stdweb;*/

fn main() {
    /*let o: String = js! {
        return prompt("offer", "");
    }
    .try_into()
    .unwrap();
    let connection = mesh::answer(std::str::from_utf8(base64::decode(&o).unwrap().as_slice()).unwrap().to_owned())
        .map_err(|e| ())
        .and_then(|peer| Ok(()));

    run(connection);*/
}
